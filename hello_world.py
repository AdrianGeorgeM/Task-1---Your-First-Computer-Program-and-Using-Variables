
# Start
# Prompt the user to enter their name and store the input in a variable called `name`.
name = input("What is your name? ")

# Prompt the user to enter their age and store the input in a variable called `age`. Convert the input to an integer.
age = int(input("How old are you? "))

# Print "Hello World!" to the console.
print("Hello World!")

# Print a formatted string to the console that includes the `name` and `age` variables.
print(f"{name} is {str(age)} years old.")
# End